# MathSolution 진행일지

---

## 2026-04-19

### 완료한 작업

- **스캔 에디터 개편 (Task 1~4 완료)**
  - `backend/data/box_templates.json`: 보기박스/조건박스 XML 템플릿 추출
  - `backend/services/hwpx_service.py`: ===조건박스=== 마커 → HWPX XML 자동 치환
  - `frontend/src/components/FormulaEditor.jsx`: HWP 수식 팔레트 + KaTeX 미리보기 + 박스 삽입 버튼
  - `frontend/src/components/ProblemReviewer.jsx`: 문제별 크롭 이미지 확인 + 포함/제외 선택
  - `frontend/src/components/TabScan.jsx`: reviewing 단계 추가 (editing → reviewing → processing)
  - `frontend/src/components/ScanResultCard.jsx`: 인라인 수식 편집 기능 추가

- **그래프 SVG 규칙 업데이트**
  - 858개 기출 그래프 이미지 분석 결과(`graph_style_report.json`) 기반
  - `backend/prompts/solve_prompt.txt` SVG 규칙 7개 → 10개로 확장
  - ⚠️ 단, 분석 결과 자체가 부정확함 (Gemini가 화살표를 "채워진 삼각형"으로 잘못 설명)

### 발견한 문제

- **현재 그래프 생성 방식(SVG)의 한계**
  - Claude가 베지어 곡선 좌표를 손으로 계산 → 수학적으로 부정확
  - 교과서 이미지(`imagesample/` 폴더)와 비교 시 품질 차이 명확
  - 교과서 그래프는 **TikZ/LaTeX**로 만든 것 (Computer Modern 폰트, 굵은 곡선 vs 얇은 축)

---

## 2026-04-21

### 작업 내용

- **anthropic_tools 패키지 완성** (`C:\Users\tnaak\anthropic_tools\`)
  - 모든 Anthropic 도구 통합 패키지 (web_search, web_fetch, code_execution, advisor, memory, bash, text_editor)
  - `pip install -e`로 모든 프로젝트에서 재사용 가능
  - `AnthropicToolsClient().use_web_search().use_advisor()` 메서드 체이닝 API

- **그래프 생성 시스템 교체 (SVG → Matplotlib)**
  - MiKTeX 설치 완료 (`C:\Users\tnaak\AppData\Local\Programs\MiKTeX`)
  - `backend/services/graph_service.py` 전면 재작성
    - SVG 직접 추출 → 구조화된 텍스트 명세 파싱 → Matplotlib STIX 폰트 PNG
    - 지원: 다항식/삼각/지수/분할함수, 채움/속빔 점, 점선 보조선, 라벨, 선분, 원
    - 화살표 축, 교과서 스타일 (STIX serif 폰트)
  - `backend/prompts/solve_prompt.txt` 9절 + 말미 그래프 규칙 섹션 교체
    - SVG 문법 완전 제거, 새 구조화 텍스트 형식으로 대체
    - 이차함수/삼각함수/지수함수 예시 포함

- **그래프 스타일 미세 조정** (commit `5edce3b`)
  - 곡선 두께 2.5 → 1.9pt (축 0.9 + 1pt로 통일)
  - 참조선(점선) 0.85 → 0.7pt
  - 틱 라벨을 tick 바깥쪽으로 빗겨가게 배치 (음수는 왼쪽/아래, 양수는 오른쪽/위)
  - 틱 숫자에 흰 배경 bbox 추가 → 점선과 겹침 방지
  - 라벨 미지정 시 함수식을 LaTeX 자동 변환해 곡선 옆에 표시 (`_expr_to_latex` 함수 추가)

- **유사문제 찾기 GUI 앱 구현 (Tasks 1~9 완료)**
  - 위치: `tools/similarity_finder/` (웹앱과 분리된 독립 폴더, Tkinter GUI)
  - Spec: [docs/superpowers/specs/2026-04-21-similarity-finder-design.md](docs/superpowers/specs/2026-04-21-similarity-finder-design.md)
  - Plan: [docs/superpowers/plans/2026-04-21-similarity-finder.md](docs/superpowers/plans/2026-04-21-similarity-finder.md)
  - 서브에이전트 기반 TDD 구동 (fresh subagent per task)
  - 진행 상황:
    | Task | 내용 | Commit |
    |------|------|--------|
    | 1 | 스켈레톤 + 프롬프트 파일 | `e5b5c7a` |
    | 2 | `build_user_message` 프롬프트 빌더 | `505768d` |
    | 3 | `parse_response` JSON 파서 | `4361b2f` |
    | 4 | `chunk_problems` + `merge_results` 배치 로직 | `5832ada` |
    | 5 | `compare` Claude API 통합 + prompt caching + 재시도 | `f7a873d` |
    | 6 | Tkinter GUI 기본 레이아웃 | `cf41172` |
    | 7 | 검색 로직 + 백그라운드 스레드 + 파싱 | `fb3e0ec` |
    | 8 | 결과 렌더링(쌍둥이/유형유사) + 복사/Opus 재확인 버튼 | `ab0d5fd` |
    | 9 | API 키 검증 + 종료 프로토콜 | `488c442` |
  - 테스트: pytest 11개 전부 통과
  - 남은 Task: 10(run.bat + E2E), 11(HWPX 추출), 12(GUI 추출 UI)

### 중간 확장 요구사항 (2026-04-21 오후)

- **HWPX 추출 기능 추가 결정** (Tasks 11~12 신설, Task 10 이후로 연기)
  - 유사문제 번호만 출력하는 것에서 끝내지 않고, **실제 HWPX 파일로 추출** 필요
  - 출력 구조: 항상 2개 HWPX 생성
    - 유사문항 파일 (유사문제만 모음)
    - 원본(나머지) 파일 (mode에 따라 내용 달라짐)
  - 두 가지 모드 지원:
    - **복사**: 유사문제가 두 파일 모두에 존재 (원본 그대로 + 유사문항 사본)
    - **잘라내기**: 유사문제가 추출본에만 존재 (원본에선 제거됨)
  - 정확 방식 채택 — HWPX XML 블록 레벨 조작 (서식/수식 그대로 보존)
  - HWPX 구조 조사 완료:
    - `Contents/section0.xml` 내 `<hp:p>` paragraph들이 순서대로
    - `<hp:endNote number="N">` 달린 paragraph = 문제 N의 끝 마커
    - `split_problems()`의 `number` = endNote 번호에 1:1 대응

### 다음 작업

1. **Task 11**: `hwpx_service.filter_hwpx_by_numbers(source_bytes, keep_numbers) -> bytes`
   - 전략: section0.xml paragraph 순회 → endNote 번호로 문제 경계 식별 → keep 집합에 있는 블록만 남기고 재조립
   - TDD: `backend/tests/test_hwpx_filter.py` 작성 예정
2. **Task 12**: GUI에 복사/잘라내기 라디오 + "HWPX로 내보내기" 버튼 추가
3. **Task 10**: `run.bat` 실행 스크립트 + 실사용 E2E 테스트
4. 실제 문제 생성 테스트 (그래프 포함 문제) — 그래프 시스템 검증용

### 참고 (현재 세션 중단 사유)

- 2026-04-21 저녁 사용량 한도 도달 (7pm KST 리셋)
- Task 11 서브에이전트 디스패치 중이었음 (`a1a7a040a0ac897a6`)
- 재개 시 이 에이전트 `SendMessage`로 이어가거나, 새 에이전트로 같은 브리프 재전달

---

## 2026-04-22

### 팀 페르소나 체계 도입

- 업무 분담을 위한 팀원 페르소나 체계 합의
  - 1조 카리나: 메인 웹앱 + Docker/배포
  - 2조 장원영: similarity_finder + hwpx 필터
  - 3조 문현빈: 손필기 생성기
  - QA 갈량: 코드 리뷰 + 일지 정리 등 횡단 업무
- 메모리(`feedback_team_personas.md`)에 반영 — 차후 세션에서도 이 체계 유지

### 2조 장원영 — 유사문제 찾기 (마무리 + 개편)

- Task 10~12 완료 (커밋 `837e50c`, `a30b577`, `1346635`)
  - `filter_hwpx_by_numbers` multi-number 버그 fix
  - `run.bat` 런처
  - HWPX 추출 UI (복사/잘라내기)
- `run.bat` 추가 수정 (커밋 `0048177`): Microsoft Store Python 스텁 회피 → `py` 런처 사용
- 숫자 변형 전용 개편 (커밋 `db0a6fb`): 프롬프트 재작성 + 유형유사 UI 제거 (유형유사는 추후 별도 기능)

### 1조 카리나 — 메인 웹앱

- Orphan 미커밋 변경사항 4개 커밋으로 분리 (`efa360a`, `677443b`, `e0d0c53`, `bf28fb7`)
  - Advisor Tool beta 통합 (`claude_service.py`)
  - 그래프 태그 SVG → 구조화 텍스트 (`solve_prompt.txt`, `graph_style_report.json`)
  - 구버전 백업 프롬프트 6개 정리
  - 메인 대시보드 학년/지침 숨김처리 (state/핸들러 보존)
- hidden + flex 클래스 충돌 방지 추가 수정 (커밋 `47dc186`)
- **대시보드 숨김 미반영 버그**: 갈량이 조사 → 원인은 Vite HMR stale이 아니라 **Docker 이미지가 4일 전 빌드된 채 실행 중** → 카리나가 `docker compose up -d --build frontend`로 재빌드 → 정상 반영 확인

### 3조 문현빈 — 손필기 생성기

- Phase 1 완료 (`feature/handwriting-generator` 브랜치, 커밋 `259de51`, `16f80ef`, `4d61440`)
  - 플랜 요약, 프로토타입 스캐폴딩 (KaTeX+나눔손글씨+Gochi Hand+Playwright), 렌더 증명 PNG
- Phase 2 완료 (`worktree-agent-ad500b44` 브랜치, 커밋 `3f2850d`, `3f1bc2b`, `f2dbadb`)
  - `handwriting/renderer.py` 모듈화 (308줄)
  - pytest suite 17건 전부 통과
  - `backend/services/handwriting_renderer.py` 래퍼 + FastAPI 라우터 드래프트 + `INTEGRATION.md`
- ⚠️ Phase 2 커밋은 별도 worktree 브랜치에 있음 — `feature/handwriting-generator`에 cherry-pick 필요 (카리나 인수)

### QA 갈량 — 코드 점검

- 대시보드 숨김 버그 근본 원인 진단: Vite dev server 재시작 권고 (실제로는 Docker 이미지 stale 이었음 — 카리나가 보완 조사)
- 부수 발견 코드 스멜 3건 (권고 수준):
  - `vite.config.js` proxy target 8000 ≠ CLAUDE.md 8001 (dead config)
  - `SettingsBar` 전체를 조건부 감쌀 수 있음 (hidden 일괄 부여보다 명시적)
  - `SettingsBar`는 `activeFeature==='create'` 탭에서만 의미 — 조건부 렌더가 단순
- 팀 페르소나 메모리 등록 + 진행일지 정리 작업 수행

### Railway 배포 사전 조사 (카리나)

- MiKTeX/LaTeX 전혀 미사용 확인 (`mathtext.fontset=stix`만 사용)
- `VITE_API_URL` env 분기 이미 존재 (localhost fallback만 주의)
- 앱이 이미 Docker Compose로 돌고 있음 — 배포 시 기존 Dockerfile 재활용 가능
- **결정 사항**: 단일 서비스(FastAPI가 프론트 정적 파일 서빙) + Railway Volume으로 히스토리 영속화 + 도메인은 나중에
- **보류**: 사용자 요청으로 Railway 배포 작업은 다음 세션으로 연기

### 다음 작업 (우선순위)

1. **카리나**: 문현빈 Phase 2 커밋 3개(`3f2850d`, `3f1bc2b`, `f2dbadb`)를 `feature/handwriting-generator`로 cherry-pick
2. **카리나**: Railway 배포 파일 작성 — Dockerfile, railway.toml, `main.py`에 StaticFiles + PORT 바인딩, 프론트 빌드 통합
3. **장원영**: 문제집 복수 파일 선택 + HWP → HWPX 자동 변환 (pywin32/한글 COM 또는 독립 변환기)
4. **문현빈 Phase 3**: HWP→LaTeX 파서 (`hwp_to_latex.py` + 기존 `hwp_math_mapping.json` 확장 + Claude fallback)
5. **문현빈 Phase 4+**: Playwright lifespan 싱글톤, 프론트 "손필기 이미지 생성" 버튼, 스타일 프리셋, PDF 출력
6. **카리나 (권고)**: 갈량이 발견한 코드 스멜 3건 정리 — `vite.config.js` proxy 포트, `SettingsBar` 조건부 렌더 단순화

### 참고 (현재 상태)

- main 브랜치 최신 커밋: `db0a6fb` (숫자 변형 전용 프롬프트 + 유형유사 UI 제거)
- `feature/handwriting-generator` 브랜치: Phase 1 커밋 3개까지 반영됨 (Phase 2는 별도 worktree)
- Docker Compose: `mathsolution-frontend` + `mathsolution-backend` 동시 기동 (5173 + 8001)
- 학년/지침 UI는 디자인 선택으로 숨김 상태 (state/핸들러는 보존되어 API 요청에 grade/guidelines 여전히 전달)

---

## 참고

- 백엔드 포트: 8001 (`--reload` 절대 금지)
- 프론트엔드 포트: 5173
- 브랜치: main
- `imagesample/` 폴더: 교과서/수능 그래프 실제 샘플 7개 (품질 기준)
